==============================
How to use 7-base_geometry.py
==============================

This module defines the BaseGeometry class.

Importing the module:
    >>> BaseGeometry = __import__('7-base_geometry').BaseGeometry

Checking class instantiation:
    >>> bg = BaseGeometry()
    >>> type(bg) == BaseGeometry
    True

Checking area() method:
    >>> try:
    ...     bg.area()
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [Exception] area() is not implemented

Checking integer_validator() with valid values:
    >>> bg.integer_validator("age", 1)
    >>> bg.integer_validator("weight", 89)

Checking integer_validator() with 0:
    >>> try:
    ...     bg.integer_validator("age", 0)
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [ValueError] age must be greater than 0

Checking integer_validator() with negative values:
    >>> try:
    ...     bg.integer_validator("age", -4)
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [ValueError] age must be greater than 0

Checking integer_validator() with string:
    >>> try:
    ...     bg.integer_validator("age", "4")
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [TypeError] age must be an integer

Checking integer_validator() with tuple:
    >>> try:
    ...     bg.integer_validator("age", (4,))
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [TypeError] age must be an integer

Checking integer_validator() with list:
    >>> try:
    ...     bg.integer_validator("age", [3])
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [TypeError] age must be an integer

Checking integer_validator() with set:
    >>> try:
    ...     bg.integer_validator("age", {3, 4})
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [TypeError] age must be an integer

Checking integer_validator() with dict:
    >>> try:
    ...     bg.integer_validator("age", {'a': 1})
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [TypeError] age must be an integer

Checking integer_validator() with float:
    >>> try:
    ...     bg.integer_validator("age", 3.14)
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [TypeError] age must be an integer

Checking integer_validator() with bool:
    >>> try:
    ...     bg.integer_validator("age", True)
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [TypeError] age must be an integer

Checking integer_validator() with None:
    >>> try:
    ...     bg.integer_validator("age", None)
    ... except Exception as e:
    ...     print("[{}] {}".format(e.__class__.__name__, e))
    [TypeError] age must be an integer

Checking integer_validator() with missing 2 arguments:
    >>> try:
    ...     bg.integer_validator()
    ... except TypeError as e:
    ...     print(e) # doctest: +ELLIPSIS
    ...
    BaseGeometry.integer_validator() missing...

Checking integer_validator() with missing 1 argument:
    >>> try:
    ...     bg.integer_validator("age")
    ... except TypeError as e:
    ...     print(e) # doctest: +ELLIPSIS
    ...
    BaseGeometry.integer_validator() missing...
